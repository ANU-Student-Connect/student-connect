// Configure accessible fields from db

const availableFields = {
  user: 'username email full_name student_id created_at last_login interests matches profile_picture is_verified notifications_enabled meetups', 
  
};
  
module.exports = availableFields;